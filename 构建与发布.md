# 构建与发布 · koishi-plugin-waygame

> 这份文档回答两件事：**怎么把 `lib/` 构建出来**，以及**发版时踩过哪些坑**。
> 动手前先看 [AGENTS.md](./AGENTS.md)（开发铁律），发布前把本文第五节「发版清单」走一遍。

---

## 一、构建

```sh
npm install     # 首次
npm run build   # → lib/
```

产物（`lib/` **已在 .gitignore 里，不进仓库**，但**必须进 npm 包**）：

| 文件 | 谁生成的 | 说明 |
| --- | --- | --- |
| `lib/index.js` | esbuild | 单文件 **CJS bundle**，`koishi` 外置（`require("koishi")`，不打进包里） |
| `lib/*.d.ts` | tsc | 类型声明（`tsconfig.json` 是 `emitDeclarationOnly: true`，只出声明不出 JS） |

构建脚本是 `scripts/build.mjs`，它其实就是这两条命令 + 一次自检：

```sh
# 1) 打包 JS
node node_modules/esbuild/bin/esbuild src/index.ts \
    --bundle --platform=node --format=cjs --target=node18 \
    --charset=utf8 --external:koishi --outfile=lib/index.js

# 2) 出类型声明（跑之前务必删掉 tsconfig.tsbuildinfo，见踩坑 ③）
node node_modules/typescript/bin/tsc -p tsconfig.json --types node
```

**为什么要有这个脚本，而不是写一行 npm script**：第 2 步前面那件事（删 `tsconfig.tsbuildinfo`）
不能用 `&&` 串起来还不难看地跨平台做；而且脚本末尾的自检能把「中文被转义了」
「`.d.ts` 没生成」这两个**静默失败**变成**构建失败** —— 否则要等包发出去才发现。

---

## 二、发布

```sh
# 0) 改版本号（会同步写进 package-lock.json）
npm version 1.0.6 --no-git-tag-version

# 1) 构建 —— 别忘了，lib/ 不在仓库里，不 build 的话包里是空的
npm run build

# 2) 测试必须全绿（当前 41 项）
npm test

# 3) 看看到底会发什么（10 个文件：lib 里 7 个 + LICENSE + readme + package.json）
npm pack --dry-run

# 4) 提交源码 + 版本号（lib/ 不进 git）
git add -A && git commit -m "chore: 版本号 1.0.6"
git push

# 5) 发布到 npm（务必指定官方源；本机默认 registry 是 npmmirror 镜像，那个只能读不能发）
npm publish --registry=https://registry.npmjs.org --access public
```

发布凭据放 `<仓库>/.npmrc`（**已在 .gitignore 里**）：

```ini
registry=https://registry.npmjs.org/
//registry.npmjs.org/:_authToken=npm_xxxxxxxx
```

> `npm publish` 返回 **202** 是正常的（"being processed"），**不是失败**。
> 注册表落地一般要 1~4 分钟，别急着重发。

---

## 三、怎么确认"发出去的包是对的"

不要只看 `npm publish` 的成功输出。真正该做的是**把发出去的那个包拉回来验**：

```sh
cd /tmp && npm pack koishi-plugin-waygame@1.0.6 && tar -xzf *.tgz
```

然后三件事：

1. **看文件齐不齐** —— 必须 10 个，尤其 `lib/index.d.ts`（`typings` 指着它；缺了的话 TS 用户装上去是没有类型的）。
2. **看中文有没有被转义** —— `lib/index.js` 里搜 `\u`，正常应该是 **0 处**（见踩坑 ①）。
3. **加载真包跑一遍** —— 隔离目录里 `require` 会因为缺 `koishi`（它是 peer 依赖）而失败，
   所以把 `lib/` 拷进本仓库再 `require`，或者给 `koishi` 打个桩：

```js
const m = require('./_pkgcheck/lib/index.js')
console.log(Object.keys(m))          // 应为 [ 'Config', 'apply', 'name', 'usage' ]
console.log(typeof Object.getOwnPropertyDescriptor(m, 'usage').get)  // 'function' → usage 是活绑定
m.apply(stubCtx, defaultConfig)      // 跑一次 apply，几秒后看 m.usage 里有没有出现热更版本
```

（`usage` 是**活绑定 getter**，所以刷新出来的版本号会直接反映到 Koishi 配置页 —— 这条值得单独验，见踩坑 ⑦。）

---

## 四、踩坑记录

> 全部是 2026-09-23 那次发 1.0.4 / 1.0.5 时真实踩到的。每条都写了「现象 → 原因 → 怎么发现的 → 怎么修」。

### ① esbuild 默认把中文转义成 `\uXXXX`

- **现象**：构建没报错，但 `lib/index.js` 里几乎没有中文，全是 `\u66f4\u65b0…` 这样的转义；
  文件从 35KB 涨到 44KB，包体积从 20KB 涨到 24KB。
- **原因**：esbuild 的 `charset` 默认是 `ascii`，非 ASCII 一律转义。tsup（Koishi 插件模板用的）默认给的是 `utf8`。
- **怎么发现的**：把**已发布的 1.0.2** 拉下来对比 —— 它裸中文 2046 个、转义 0 个，我构建的正好相反。
- **怎么修**：加 `--charset=utf8`。运行时两者等价（JS 引擎解析时会把转义还原），
  但没理由和已发布的包搞成两套风格，而且白白大一截。
- **防呆**：`scripts/build.mjs` 的自检会数 `\uXXXX`，**有一处就构建失败**。
  > 这条防呆自己也踩过一次坑：第一版写的是「转义 > 0 **且** 裸中文 < 100」，
  > 结果 ascii 构建其实是「转义 2322 处、裸中文仍有 202 个」，**没低于 100，直接漏过去了**。
  > 是用"故意去掉 `--charset=utf8` 再跑一遍"的反向测试当场抓出来的。
  > 教训：**判据别用阈值猜**。这个项目正常构建时转义数精确等于 0，那就按 0 判。

### ② `tsconfig` 里的 `yml-register/types` 找不到

- **现象**：`npx tsc` 报 `TS2688: Cannot find type definition file for 'yml-register/types'`。
- **原因**：`tsconfig.json` 的 `compilerOptions.types` 里有 `yml-register/types`，
  那是 **Koishi 应用工作区**里的包；本仓库单独 clone 出来没有它。
- **怎么修**：命令行加 `--types node` 顶掉整个 types 数组。
  （注意这只影响声明生成；`npm test` 走 tsx 不做类型检查，所以一直是绿的，别被这个假象骗了。）

### ③ `tsconfig.tsbuildinfo` 让 tsc **静默**不生成 `.d.ts` ★最阴的一个

- **现象**：`tsc` **退出码 0、一行输出都没有**，但 `lib/` 里只有 `index.js`，
  `config.d.ts` / `index.d.ts` 等一个都没有。
- **原因**：`tsconfig` 开了 `composite: true` + `incremental: true`。
  仓库根目录的 `tsconfig.tsbuildinfo` 记录了上次的构建状态，源码没变它就认为"已是最新"，**跳过 emit**。
  而我当时是先 `rm -rf lib` 再跑 tsc —— 删了产物却没删 buildinfo，正好踩中。
- **怎么发现的**：构建完 `Get-ChildItem lib` 发现只有 1 个文件。**要是没看这一眼，
  发出去的包就没有 `.d.ts`，而 `package.json` 的 `typings` 正指着 `lib/index.d.ts`。**
- **怎么修**：构建前 `rm -f tsconfig.tsbuildinfo`（`scripts/build.mjs` 里第一步就是它）。
- **防呆**：自检里断言 `lib/index.d.ts` 存在。

### ④ `lib/` 不在仓库里，发布前必须 build

- **现象**：clone 下来直接 `npm publish`，包里除了 LICENSE/readme/package.json 什么都没有。
- **原因**：`.gitignore` 挡着 `lib`（构建产物不进版本库），但 `package.json` 的 `files` 要有它。
- **怎么修**：发布前一定先 `npm run build`；`npm pack --dry-run` 确认是 10 个文件。

### ⑤ Node 20+ 不许直接 spawn `.cmd`

- **现象**：构建脚本里 `execFileSync(node_modules/.bin/esbuild.cmd, …)` 抛
  `Error: spawnSync … EINVAL`。
- **原因**：Node 20 起（CVE-2024-27980）禁掉了不带 `shell: true` 的 `.cmd`/`.bat` 执行；
  而 `node_modules/.bin` 在 Windows 上就是 `.cmd`。
- **怎么修**：**不用 `.bin`**，直接用 node 跑包里的 JS 入口：
  `execFileSync(process.execPath, ['node_modules/esbuild/bin/esbuild', …])`。
  比 `shell: true` 安全（不会把带空格/中文的参数当命令解析），而且三平台一致。

### ⑥ npm token：能 publish，不能 unpublish

- **现象**：发错了想撤回，`npm unpublish koishi-plugin-waygame@1.0.4 --force` 报
  `403 … Granular access tokens that bypass two-factor authentication may not perform this action`。
- **原因**：绕过 2FA 的细粒度 token 被禁止执行破坏性操作（unpublish / 改 dist-tag 等）。
- **怎么修**：要么在 npm 网页上删，要么换一个带 OTP 的登录态。
  **发版前多花 30 秒 `npm pack --dry-run`，比事后撤回省事得多。**

### ⑦ 判断"原来是怎么构建的"，最快是看已发布的包

- **背景**：仓库里没有 `build` 脚本，`tsconfig` 又是 `emitDeclarationOnly`，
  看不出 JS 是谁产出的（`AGENTS.md` 只说"构建在 Koishi 应用根目录做"）。
- **做法**：`npm pack koishi-plugin-waygame@<上一个版本>` → 解开看 `lib/`。
  一眼就能看出是 **esbuild/tsup 的单文件 CJS bundle**（`__defProp`/`__export`/`__toCommonJS` 是 esbuild 的标志），
  再加 `lib/*.d.ts`。**先看产物再动手，比猜快得多** —— 这次就是没先看，才先构建出了 charset 不对的一版。

### ⑧ `npm view` 有缓存，发布后别急着判断"没上去"

- **现象**：`npm publish` 返回 202、退出码 0，但 `npm view koishi-plugin-waygame version` 还是旧版本，
  `npm view pkg@新版本` 直接 404。
- **原因**：注册表要 1~4 分钟才把新版本写进 packument；`npm view` 还可能吃本地缓存。
- **怎么修**：直接查 registry 接口并破缓存：
  ```pwsh
  (Invoke-RestMethod "https://registry.npmjs.org/koishi-plugin-waygame?t=$([DateTime]::UtcNow.Ticks)").'dist-tags'.latest
  ```

### ⑨ 别把本地默认 registry 当发布目标

- 本机 `npm config get registry` 是 `https://registry.npmmirror.com`（只读镜像，**发不上去**）。
  发布时必须 `--registry=https://registry.npmjs.org`，或者像现在这样在仓库 `.npmrc` 里写死。

---

## 五、发版清单

- [ ] 改动都提交了，`npm test` **41 项全绿**
- [ ] `npm version <新版本> --no-git-tag-version`
- [ ] `npm run build`（自检通过：`lib/index.js` + `lib/*.d.ts` 齐全、无中文转义）
- [ ] `npm pack --dry-run` = **10 个文件**
- [ ] `git push`（源码 + 版本号；`lib/` 不进库）
- [ ] `npm publish --registry=https://registry.npmjs.org --access public`
- [ ] 等 1~4 分钟，确认 `dist-tags.latest` 已经指到新版本
- [ ] 把发出去的包拉回来，按第三节验一遍（文件齐 / 无转义 / 真包能加载并能刷出热更版本）
